$(function() {
    var w = Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
    if (w < 1200) {
        window.location = "https://github.com/nint8835/iTunesRichPresence";
    }
});